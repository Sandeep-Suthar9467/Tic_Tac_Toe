import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    header: {
        fontSize: 24,
        color: "white",
        position: "absolute",
        top: 50,
      }
  });
  