import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    circle: {
      flex: 1,
      borderRadius: 50,
      alignItems: "center",
      justifyContent: "center",
      margin: 10,
  
      borderWidth: 10,
      borderColor: "white",
    },
  });
  